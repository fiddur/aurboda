# Database
export PGUSER=
export PGDATABASE=
export PGHOST=
export PGPASSWORD=

# External services
export OURA_CLIENT=
export OURA_SECRET=
export RESCUETIME_KEY=

# Backend server
export PORT=3000
export WEB_HOST=http://localhost:5173
# API_BASE_URL — the public URL where the backend API is reachable (for OAuth callbacks, webhooks).
# In Docker/production this defaults to ${WEB_HOST}/api. For local dev, set explicitly:
export API_BASE_URL=http://localhost:3000

# Frontend (Vite)
VITE_API_URL=http://localhost:3000

# Development override - mounts source code for hot reload
services:
  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
      target: builder
    command: pnpm --filter aurboda-backend watch
    volumes:
      - ./apps/backend/src:/app/apps/backend/src:ro
    environment:
      - NODE_ENV=development
